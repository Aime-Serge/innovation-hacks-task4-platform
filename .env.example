# Copy to .env (never commit it). `make env` fills the secrets with random values.
POSTGRES_DB=ih_platform
POSTGRES_USER=ih_admin
POSTGRES_PASSWORD=<set-me>
APP_DB_PASSWORD=<set-me>
MIGRATOR_DB_PASSWORD=<set-me>
READONLY_DB_PASSWORD=<set-me>
JWT_SECRET=<set-me>
SEED_PROFILE=none
SEED_PASSWORD=

# Minimal profile (feat/minimal-profile). Not secrets; the API reads them (pack section 5).
# Minimum age the registration checkbox confirms (default 16); no birth date is collected.
MIN_AGE=16
# The current terms version stored with each registration (existing users are backfilled as "legacy").
TERMS_VERSION=2026-09
