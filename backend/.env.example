APP_ENV=development
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info

# SQLAlchemy connection string for the Postgres instance backing all
# persistence. Never commit a real value here. Local dev (Docker):
#   postgresql+psycopg2://postgres:devpassword@localhost:5432/ih_task4
DATABASE_URL=

# Signs auth JWTs — required, no insecure default. Generate one with:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=

# Comma-separated list of frontend origins allowed to call this API with
# credentials (cookies). Local dev default matches `npm run dev`.
CORS_ORIGINS=http://localhost:3000

# Optional. Powers the AI-assisted task generation endpoint
# (POST /projects/{id}/ai/generate-tasks). When unset, that endpoint still
# works — it falls back to a deterministic, templated task breakdown
# instead of calling the Gemini API. Gemini specifically because it has
# a genuine free tier — get a key at https://aistudio.google.com/apikey
# with no billing setup required.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
