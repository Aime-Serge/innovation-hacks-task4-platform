APP_ENV=development
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info

# Required as of Task 3 — SQLAlchemy connection string for the Postgres
# instance backing all persistence. Never commit a real value here; this
# file only documents the key. Local dev (Docker):
#   postgresql+psycopg2://postgres:devpassword@localhost:5432/ih_task3
DATABASE_URL=

# Placeholder for Task 4 (authentication) — unused until auth lands.
SECRET_KEY=
