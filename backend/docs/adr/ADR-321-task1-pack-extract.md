# ADR-321: the Task 1 pack could not be re-counted

**Status:** Accepted as a recorded gap.

The pack asks for the Task 1 counts (24 functional, 24 non-functional) to be confirmed. The copy kept in `docs/standards/` is a text extract that lost its tables: only 21 functional and 16 non-functional identifiers are recoverable from it. Task 3 does not depend on those counts. Task 1 compatibility is checked against the dashboard's own types in `docs/compatibility-task1.md`, and the OpenAPI document is unchanged from the baseline apart from the documented readiness details.
