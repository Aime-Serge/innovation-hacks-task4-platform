# ADR-401: Vercel for the frontend, Render for the API and database; the browser talks only to Vercel

**Status:** Accepted (from the Task 4 pack, section 7).

**Reason.** Team's choice from the guide's list; first-party cookies and no exposed API address

**Where it lives.** `render.yaml`, `frontend/vercel.json`, `frontend/src/app/api/bff`.
