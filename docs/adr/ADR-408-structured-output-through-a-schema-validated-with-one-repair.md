# ADR-408: Structured output through a schema, validated, with one repair attempt

**Status:** Accepted (from the Task 4 pack, section 7).

**Reason.** Reliable machine-readable results

**Where it lives.** `backend/app/ai/schemas.py`, `service.py`.
