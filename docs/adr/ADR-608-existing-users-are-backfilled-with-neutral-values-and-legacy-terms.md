# ADR-608: Existing users are backfilled with neutral values and `legacy` terms

**Status:** Accepted (from the Minimal Profile pack, section 9).

**Reason.** No data loss and honest about what is unknown: discipline `other`, seniority `mid`, employment `between_roles`, country `ZZ`, time zone `UTC`, terms `legacy`, age confirmation left empty.

**Where it lives.** Migration `0008_minimal_profile` (to be confirmed).
