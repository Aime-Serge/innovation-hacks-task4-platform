# LinkedIn post draft — Task 4 (Capstone)

Post this once the demo video is recorded and uploaded (and, if
deployed, once the live link is confirmed working). Attach the video —
or a couple of screenshots from `frontend/docs/screenshots/` if it's not
ready yet — and tag Innovation Hacks using LinkedIn's own @-mention as
you type (select their page from the mention dropdown; typing
"@Innovation Hacks" as plain text does not create a real tag).

## Post copy

Just wrapped up the capstone of my Full Stack Development Internship
with Innovation Hacks: an AI-powered project and task management
platform, built end-to-end over four connected tasks.

What it does:
🔐 Real authentication — Argon2id password hashing, httpOnly JWT
sessions, CSRF-hardened
📊 A live dashboard, full project & task CRUD — assign, prioritize, set
due dates, search & filter
🤖 One real AI feature: task generation via the Anthropic API, with an
honest fallback so it never breaks
🗄️ Postgres-backed, with a schema and migrations built from scratch

Stack: Next.js, TypeScript, Tailwind, FastAPI, PostgreSQL, SQLAlchemy,
Anthropic API.

The build went through a full security review before deployment — found
and fixed a real CSRF gap, not just a checklist pass.

Repo: [link]
Demo: [link]
Live: [link, if deployed]

@Innovation Hacks #FullStackDevelopment #BuildInnovateImpact
