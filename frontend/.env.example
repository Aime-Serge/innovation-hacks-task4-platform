# Local dev: base URL of the Task 4 backend API, no trailing slash.
# Production (Vercel): set this to /api instead — API calls are proxied
# same-origin through Next.js so the session cookie stays first-party
# (see next.config.ts for why).
NEXT_PUBLIC_API_URL=http://localhost:8000

# Production only. Server-side (not exposed to the browser): the API's
# real base URL that /api/* is proxied to, e.g. https://your-api.onrender.com
# Leave unset locally.
API_PROXY_TARGET=

# Production only, optional. Public URL of the API's Swagger docs, used by
# the footer's "API docs" link, e.g. https://your-api.onrender.com/docs
NEXT_PUBLIC_API_DOCS_URL=
