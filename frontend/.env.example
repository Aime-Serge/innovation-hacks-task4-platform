# Server-only settings of the Next.js server layer (section 8). None starts with NEXT_PUBLIC_, so
# none can reach the browser bundle. Copy to .env.local and fill in; real values are never committed.

# The Render API, used only by /api/bff. https in production.
API_BASE_URL=<set-me>
# The site's own origin, for the Origin check on state-changing calls. https in production.
SITE_URL=<set-me>
# Upstream timeout of the server layer, in milliseconds (1000 to 60000).
BFF_TIMEOUT_MS=28000

# Local development over plain HTTP only. Production refuses both lines: leave them unset there.
# APP_ENV=development
# ALLOW_INSECURE_COOKIES=true

# Tests only: serve the Task 1 mock adapter instead of the real API.
# NEXT_PUBLIC_DATA_SOURCE=mock
