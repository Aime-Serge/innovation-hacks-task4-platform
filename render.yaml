databases:
  - name: ih-task4-db
    plan: free
    databaseName: ih_task4
    user: ih_task4

services:
  - type: web
    name: ih-task4-api
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: "pip install -r requirements.txt && alembic upgrade head"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    healthCheckPath: /health
    envVars:
      - key: APP_ENV
        value: production
      - key: LOG_LEVEL
        value: info
      # DB connection — provisioned and injected by Render itself from the
      # database resource above, never typed in by hand.
      - key: DATABASE_URL
        fromDatabase:
          name: ih-task4-db
          property: connectionString
      # Everything below is a real secret: set once in the Render
      # dashboard's environment tab (its own secret manager), never
      # committed here. `sync: false` is what tells Render "this value
      # lives in the dashboard, not in this file."
      - key: SECRET_KEY
        sync: false
      - key: CORS_ORIGINS
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: GEMINI_MODEL
        sync: false
      # Not secret, but must be set to the real Vercel URL in production
      # — left at the code default (localhost), password-reset links
      # would point at a dev server nobody but the developer can reach.
      - key: FRONTEND_URL
        sync: false
